# Database Configuration
DB_USER=postgres
DB_PASSWORD=4UuA6hDEqzkLZD
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agent_server

# Embedding Service Configuration
# Hugging Face Text Embeddings Inference (TEI) endpoint for embeddings
EMBEDDING_ENDPOINT=http://localhost:8605

# Reranker Service Configuration
# Hugging Face Text Embeddings Inference (TEI) endpoint for reranking
RERANK_ENDPOINT=http://localhost:8606
